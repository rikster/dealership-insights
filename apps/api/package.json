{
  "name": "@autograb/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/server.ts",
    "build": "tsc --noEmit",
    "start": "tsx --env-file=../../.env src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@autograb/contracts": "workspace:*",
    "@autograb/db": "workspace:*",
    "@autograb/orchestrator": "workspace:*",
    "@fastify/cors": "latest",
    "fastify": "latest",
    "fastify-type-provider-zod": "latest",
    "zod": "latest"
  },
  "devDependencies": { "tsx": "latest", "typescript": "~6.0.2", "vitest": "latest" }
}
